apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-codelet-application-service
spec:
  selector:
    app: {{ .Release.Name }}-codelet-application-service
  ports:
    - port: {{ .Values.container.containerPort }}
      targetPort: {{ .Values.container.containerPort }}
      nodePort: {{ .Values.container.containerPort }}